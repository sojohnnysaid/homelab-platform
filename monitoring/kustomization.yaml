apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # grafana.yaml contains: Grafana deployment, prometheus-config, grafana-dashboards
  - grafana.yaml
  - alertmanager.yaml
  - alert-rules.yaml
  - failover-alert-rules.yaml
  - cloudflared-alert-rules.yaml
  - loki-stack.yaml
  - node-exporter.yaml
  - kube-state-metrics.yaml
  - blackbox-exporter.yaml
  - persistent-volumes.yaml
  - daily-alert-digest.yaml
  # Removed duplicate ConfigMaps:
  # - prometheus-config.yaml (in grafana.yaml)
  # - dashboards-configmap.yaml (in grafana.yaml)
  # - enhanced-dashboards-configmap.yaml (duplicate dashboards)
